2) Associative array (named / key-value)

declare -A ntasks
ntasks["vasp"]=32
ntasks["qe"]=64
ntasks["lammps"]=8
___________________
for loop:
`````````
for i in condition;do
	command
done
-----
# for i in {1..10};do echo "Out: ${i}";done
-----
for ((i=0;i<=10;i++));do echo "LOL";done
-----


for ((i=10;i>=1;i--));do
        for ((j=1;j<=$i;j++));do
                echo -n "*"
        done
        echo ""
done

______________

declare -A arr=(["Name"]="Dheeraj" ["Love"]="Coffee" ["Module"]="Linux")

for i in ${!arr[@]};do
        echo "Key=${i}, Value=${arr[${i}]}"
done
